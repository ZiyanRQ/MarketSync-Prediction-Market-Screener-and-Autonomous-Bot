# Day 005 - Tooling Transition & Roadmap Planning

**Date:** 6 July 2026

---

# Time Spent

- 🎮 Farmer Was Replaced: None
- 📚 Educative/DataCamp: None
- 📝 Planning & Documentation: ~30–45 Minutes
- ⏱️ Total Time: ~30–45 Minutes

---

# Goals

## Today's Goals

- Transition devlog documentation workflow from ChatGPT to the AI assistant.
- Solve the cross-conversation memory/sync problem I was having with ChatGPT.
- Lock in a clear roadmap from current Python skill level → working arbitrage screener.
- Not write code today — too late in the day — just plan.

---

# What I Built

No code today. This session was entirely about fixing my documentation workflow and mapping out the path forward.

## The Problem

ChatGPT was generating my devlogs (days 001–004) based on information I gave it each session, but it struggled to stay consistent across separate conversations within the same project. Context didn't reliably carry over, which made the devlogs inconsistent over time.

## The Switch

Starting today, I'm using the AI assistant for this instead. The new workflow:

1. I paste rough, unstructured notes about what I did/failed/learned during a session.
2. The AI assistant condenses that into a structured devlog entry matching the format of days 001–004.
3. The AI assistant has direct access to my past devlog files for context, rather than relying on memory of a single conversation thread.
4. Going forward, the AI assistant will also act as a Socratic debugging partner for the screener project — pointing me toward concepts and flagging issues in my logic rather than handing me finished code, so I actually learn the implementation myself.

---

# The Project: Recap & Direction

## Final Goal

Build a **market screener** in Python that compares YES/NO prices for matching events across **Kalshi** and **Polymarket**, and flags price discrepancies where combined cost (YES on one side + NO on the other) is under $1 — a guaranteed arbitrage spread before fees.

Example: YES at 60¢ on one platform + NO at 30¢ on the other = 90¢ total cost, guaranteed $1 payout = 10¢ profit minus fees.

## Scope Note

This project is scoped as a **screener only** — identifying and displaying price discrepancies, not acting on them. I'm UK-based, and jurisdictional/eligibility restrictions may apply to actually trading on these platforms, so I'm deliberately keeping the project to data comparison and analysis for now rather than building anything that places trades.

## Explicitly Out of Scope

- Automated cross-platform market matching (too complex to solve alongside the arbitrage logic itself — matching will be done manually/hardcoded for now).

## Key Technical Findings From Research

- **Kalshi**: all market data (prices, order books, market metadata) is publicly available without authentication. Auth is only needed later, for placing trades.
- **Polymarket**: split into separate APIs — Gamma (public, market discovery/metadata) and CLOB (order book + trading, auth required only for placing orders). For a screener, I only need Gamma.
- **Practical implication**: no account verification or API keys are needed to start building the screener. I can start hitting public endpoints on both platforms immediately once my Python fundamentals are solid enough.

## Market Matching Strategy

Rather than trying to auto-match questions by text/keywords (a hard NLP problem on its own), I'll:

1. Manually pick 5–10 markets where I can visually confirm both platforms are tracking the exact same resolution event (same date, same source, same threshold).
2. Hardcode the mapping (e.g. Kalshi ticker ↔ Polymarket condition ID) in a small config/dict.
3. Build and test the screener logic against that fixed list first.
4. Treat automated market-matching as a possible future stretch goal, not a current requirement.

---

# Python Learning Plan

## Current Status

Completed via DataCamp (before trial expiry):

- Python Basics
- Python Lists
- Functions and Packages
- NumPy
- Matplotlib
- Dictionaries & Pandas

**Confirmed gap:** Logic/Control Flow, Loops, and the Hacker Statistics case study were never completed on DataCamp (0%).

## Transition to Educative (via GitHub Student Developer Pack)

To avoid re-doing material I've already covered, I mapped DataCamp's completed modules against Educative's course structure:

**Skip (already covered):**
- Basic Data Types
- Lists
- Modules and Functions

**Skim only (partial overlap):**
- Dictionaries — I covered this via DataCamp's Pandas-focused lens; Educative's version may go broader (comprehensions, nested structures) so a quick pass makes sense, not a full redo.

**Priority — genuinely new material:**
- **Iteration & Loops** — confirmed gap from DataCamp, and foundational to every part of the screener (looping over markets, polling both platforms).
- **Classes** — never covered. Relevant for structuring screener data cleanly (e.g. a `Market` or `PriceQuote` class) instead of loose dictionaries.
- **Asynchronous Programming** — higher priority than it looks. The screener needs to fetch prices from two platforms; doing this synchronously risks price drift between reads. Async lets both fetches happen concurrently.
- Iterators / Generators — lower priority, useful for cleaner data processing later.

**Planned order:** Iteration & Loops → Classes → Asynchronous Programming → (later) Iterators/Generators/Dictionaries-skim.

## Mini Projects (Before Returning to the Screener)

Small projects to build API/data-handling muscle before tackling the full screener.

### First: Spotify API Integration (Educative course)

This is a separate, dedicated course within Educative ("Integrate the Spotify API in Python") rather than a from-scratch build. It'll be my first project since it comes as a guided walkthrough — good for a first exposure to OAuth/token-based authentication with structured support, before I try authentication or API work fully unassisted.

### Then: From-Scratch Mini Projects

After finishing the guided Spotify course, I'll build the rest fully from scratch to reinforce the concepts without a walkthrough:

1. **Weather app** — first unassisted exposure to GET requests, JSON parsing, API keys/env variables.
2. **Crypto tracker** — polling multiple sources, looping over a list of assets — closest structural analog to the screener's core loop.
3. **Stock price viewer** — working with price data, comparison logic.

Design constraint to apply across all of these: keep "fetch data" and "process/display data" as separate functions, the same separation-of-concerns instinct I started applying with `smartplant` in Day 004. This directly mirrors the shape the screener will need (`get_price(source)` as its own function, called once per platform, then compared separately).

No specific weather API chosen yet — to be decided when I actually start that project.

---

# Programming Concepts Learned

Today wasn't a coding day, but I did clarify:

- The difference between a hard requirement and a "nice to have later" (auto market-matching vs manual mapping for now).
- Why async matters specifically for a two-source comparison tool, not just as a general Python topic.
- The value of scoping a project properly before writing code, so the deliverable stays achievable.

---

# Challenges

## Challenge 1 - Documentation Continuity

### Problem

ChatGPT's devlog generation lost consistency across separate conversations, making days 001–004 harder to keep aligned.

### Solution

Switched to the AI assistant, which can directly reference my existing devlog files for context and maintain consistency going forward.

### Lesson Learned

Tooling matters for process, not just for code — an unreliable documentation workflow costs real time even when the underlying project work is solid.

---

# Things To Improve

- Actually start the Iteration & Loops module on Educative.
- Pick a free weather API and start the first mini project.
- Keep devlogs updated in this new format/workflow consistently.

---

# Biggest Lessons

- Keeping the project clearly scoped as a screener — data comparison, not trading — keeps it achievable and avoids getting tangled in jurisdictional/eligibility questions I'd need to solve separately.
- Manually mapping markets first is a legitimate engineering decision, not a shortcut — worth stating clearly for the portfolio narrative.
- Async isn't just a "nice to know" topic for this project — it's directly load-bearing for accurate price comparisons.

---

# Daily Wins

- ✅ Solved the documentation continuity problem by switching tools.
- ✅ Confirmed exact Python skill gaps (Loops, Control Flow, Classes, Async).
- ✅ Mapped DataCamp progress against Educative's course structure to avoid wasted repetition.
- ✅ Locked in a clear, scoped roadmap from current skill level to a working screener.
- ✅ Clarified Kalshi/Polymarket API access requirements — no auth needed for a screener.
- ✅ Defined a manual market-matching strategy.

---

# Tomorrow's Goals

- Start the Iteration & Loops module on Educative.
- Pick a free weather API for the first mini project.
- Begin the weather app mini project if time allows.

---

# Reflection

Today was a planning day rather than a building day, and that's fine — the project needed it. Days 001–004 were mostly about learning syntax and experimenting inside Farmer Was Replaced, but the arbitrage screener is a real-world, multi-API project, and going in without a clear roadmap would have wasted time later.

Switching my documentation workflow to the AI assistant also solves a real problem rather than just being a tooling preference — consistent context across sessions matters when a project spans weeks or months. From here, the plan is concrete: close the Loops/Classes/Async gap, build a couple of small API-based projects to get comfortable with the exact skills the screener needs, then come back and build the real thing.